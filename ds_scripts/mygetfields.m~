function [vals] = mygetfields(structs, fields)
  if (!iscell(fields))
    fields = {fields};
  endif

  vals = cellfun("mygetfield",{structs},fields,"UniformOutput",false)';
  if (all(cellfun("isnumeric",vals)))
    vals = cell2mat(vals);
  endif
end
